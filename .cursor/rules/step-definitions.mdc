---
description: Step definition conventions for Cucumber BDD steps
globs: src/steps/**/*.ts
alwaysApply: false
---

# Step Definition Conventions

## Structure

```typescript
import { Then } from '@cucumber/cucumber';
import { ModulePage } from '../pages/module.page';
import { ExtentTestManager } from '../utils/extent-test-manager';
import { PlaywrightWorld } from '../support/world';

const getModulePage = (world: PlaywrightWorld) => new ModulePage(world.page);

Then('Step description here', async function (this: PlaywrightWorld) {
    await getModulePage(this).doSomething();
    ExtentTestManager.logPass('What passed');
});
```

## Rules

- Always type `this` as `PlaywrightWorld` in every step function
- Always call `ExtentTestManager.logPass(...)` at the end of a passing step
- Use `throw new Error(...)` for failures — the hook will capture screenshots automatically
- Store scenario state on `this['key']` — not in module-level variables
- Instantiate page objects via a helper function at the top of the file
- Do not put raw XPath selectors in step files — delegate to page objects
- When saving test data, use `saveNameEntries` for multiple keys (atomic write)
- Reuse existing steps by matching exact wording — check feature files before creating duplicates

## Naming

- Step text should read like natural English: `Then I should see the "Audience" page`
- Use `Click the X button` for click actions
- Use `Verify X should Y` for assertions
- Use `Enter the X` for fill actions
- Parameterise with Cucumber expressions: `{int}`, `{string}`, `{word}`

## World State Keys

| Key | Set by | Used by |
|-----|--------|---------|
| `currentAudienceTitle` | Audience creation steps | Filter/search/verify steps |
| `currentCampaignName` | Campaign creation steps | Search/verify steps |
| `existingAudienceTitle` | Precondition steps | Part-of-audience, campaign steps |
| `selectedAudienceName` | Audience selection steps | Verification steps |
| `duplicatedAudienceTitle` | Duplicate steps | Filter steps |
| `updatedCampaignName` | Edit name steps | Search/verify steps |
| `scenarioTag` | Hooks (Before) | Tag-based save steps |
| `scenarioTags` | Hooks (Before) | Tag-based conditional logic |
